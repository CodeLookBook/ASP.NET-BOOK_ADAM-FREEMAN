@model IEnumerable<Product>

@{
    ViewData["Title"] = "View";
}

@foreach (Product product in Model)
{
    <h3>@product.Name</h3>
    @product.Description
    <h4>@product.Price.ToString("c")</h4>
}

